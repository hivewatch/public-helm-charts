apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "clamav_mirror.fullname" . }}-test-connection"
  labels:
    {{- include "clamav_mirror.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "clamav_mirror.fullname" . }}:{{ .Values.service.api.port }}']
  restartPolicy: Never
